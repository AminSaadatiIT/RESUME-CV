# Agent Instructions

## Environment

- Windows environment with **no bash available** — `run_terminal_command` will fail with "Bash is required but was not found on this Windows system." Install Git for Windows (which includes Git Bash) or set `CODEBUFF_GIT_BASH_PATH` to a valid bash.exe path to enable terminal commands.
